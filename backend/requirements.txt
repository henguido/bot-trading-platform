# Dependencias del backend.
#
# Instalar con:             pip install -r backend/requirements.txt
# Instalacion REPRODUCIBLE: pip install -r backend/requirements.txt -c constraints.txt
#
# constraints.txt (en la raiz) fija el conjunto exacto verificado. Sin el,
# pip resuelve la version mas reciente de cada paquete y dos reconstrucciones
# pueden instalar conjuntos distintos.

fastapi
uvicorn
sqlalchemy
python-dotenv
python-binance
aiohttp
cryptography
requests
passlib[bcrypt]
# passlib 1.7.4 no es compatible con bcrypt 5.x en su backend probe.
# El limite se declara tambien aqui para que una instalacion SIN constraints
# no vuelva a romper hash/verify de contrasenas.
bcrypt<5
python-jose[cryptography]
psycopg2-binary
email-validator

# Dependencias que el codigo importa DIRECTAMENTE y que hasta ahora solo
# llegaban por transitividad. Declararlas evita que una reconstruccion futura
# se rompa si el paquete intermedio deja de arrastrarlas.
#
#   pytz      importado por backend/main.py, backend/risk/reloj.py y
#             backend/utils/time_utils.py. Sostiene el calculo del dia de
#             riesgo y, por tanto, el kill switch diario.
#             Llegaba via dateparser <- python-binance.
#   pydantic  importado por backend/app/schemas.py. Llegaba via fastapi.
#
# Sin pin, igual que el resto de este archivo. Versiones de referencia:
#   pytz==2026.3.post1   pydantic==2.13.4
pytz
pydantic